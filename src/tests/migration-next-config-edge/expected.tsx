// TODO(tanstack-migrate): next.config semantics detected (i18n, trailingSlash); migrate these settings manually.
const nextConfig = {
  i18n: { locales: ["en"], defaultLocale: "en" },
  trailingSlash: true,
};

export default nextConfig;
